'use strict';
const $=id=>document.getElementById(id),apiBase=new URL('../api/',document.currentScript.src);let access=sessionStorage.getItem('edumaster-access')||localStorage.getItem('edumaster-access')||'';
async function request(path,options={}){const response=await fetch(new URL(path,apiBase),{...options,headers:{Authorization:'Bearer '+access,...options.headers}});if(response.status===401)throw Object.assign(new Error('비밀번호를 확인해 주세요.'),{unauthorized:true});if(!response.ok){let data={};try{data=await response.json();}catch{}throw new Error(data.error||'서버 응답을 확인하지 못했습니다.');}return response;}
function showLogin(message=''){$('login').hidden=false;$('archive').hidden=true;$('login-error').textContent=message;}
function formatSize(bytes){return bytes<1024*1024?Math.max(1,Math.round(bytes/1024))+' KB':(bytes/1024/1024).toFixed(1)+' MB';}
async function openPdf(item,download=false){const viewer=download?null:window.open('about:blank','_blank');try{$('archive-status').className='status';$('archive-status').textContent=(download?'다운로드':'PDF 열기')+' 준비 중…';const response=await request(`reports/${item.id}/file${download?'?download=true':''}`),blob=await response.blob(),url=URL.createObjectURL(blob);if(download){const a=document.createElement('a');a.href=url;a.download=item.title+'.pdf';a.click();setTimeout(()=>URL.revokeObjectURL(url),30000);}else if(viewer){viewer.location.href=url;setTimeout(()=>URL.revokeObjectURL(url),120000);}else{location.href=url;}$('archive-status').textContent=download?'다운로드를 시작했습니다.':'새 창에서 PDF를 열었습니다.';}catch(error){viewer?.close();$('archive-status').className='status error';$('archive-status').textContent=error.message;}}
function render(items){$('count').textContent=`${items.length}개`;$('empty').hidden=items.length>0;$('list').replaceChildren(...items.map(item=>{const row=document.createElement('article'),info=document.createElement('div'),title=document.createElement('h2'),meta=document.createElement('div'),actions=document.createElement('div'),view=document.createElement('button'),download=document.createElement('button');row.className='item';title.textContent=item.title;meta.className='meta';meta.textContent=`${new Date(item.createdAt).toLocaleString('ko-KR')} · ${formatSize(item.size)}`;info.append(title,meta);actions.className='actions';view.className='primary';view.textContent='보기';download.textContent='다운로드';view.onclick=()=>openPdf(item);download.onclick=()=>openPdf(item,true);actions.append(view,download);row.append(info,actions);return row;}));}
async function load(persist=false){try{const response=await request('reports'),items=await response.json();if(persist)localStorage.setItem('edumaster-access',access);$('login').hidden=true;$('archive').hidden=false;$('archive-status').textContent='';render(items);}catch(e){if(e.unauthorized)showLogin(e.message);else showLogin('목록을 불러오지 못했습니다. '+e.message);}}
$('login-form').onsubmit=async event=>{event.preventDefault();access=$('password').value;await load(true);};$('refresh').onclick=()=>load();load();
